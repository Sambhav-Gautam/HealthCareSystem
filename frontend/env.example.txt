# Frontend Environment Configuration
# Copy this file to .env and update as needed

# API Gateway URL (Microservices Architecture)
# The frontend communicates with the API Gateway, which routes to Auth and Medical services
VITE_API_URL=http://localhost:3000/api

# For production deployment, update to your production API Gateway URL:
# VITE_API_URL=https://your-api-gateway-url.com/api

