# Medical Service Configuration
# Copy this file to .env and fill in your actual values

# Server
PORT=5002
NODE_ENV=development

# MongoDB - Medical Database
MONGO_URI=mongodb://localhost:27018/hcl_medical_db

# Auth Service URL (for token verification)
AUTH_SERVICE_URL=http://localhost:5001

# Encryption Key (use libsodium to generate)
# Generate with: node -e "const sodium = require('libsodium-wrappers'); (async () => { await sodium.ready; const key = sodium.randombytes_buf(sodium.crypto_secretbox_KEYBYTES); console.log(sodium.to_hex(key)); })();"
ENCRYPTION_KEY=your_generated_encryption_key_here

# Email Configuration (Nodemailer with Gmail)
EMAIL_SERVICE=gmail
EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=587
EMAIL_USER=your_email@gmail.com
EMAIL_PASSWORD=your_app_specific_password
EMAIL_FROM=HCL Healthcare <your_email@gmail.com>

# CORS Configuration
CORS_ORIGIN=http://localhost:5173,http://localhost:3000

# Rate Limiting
GENERAL_RATE_LIMIT_WINDOW=15
GENERAL_RATE_LIMIT_MAX=100

# Cron Job Settings
CRON_APPOINTMENT_REMINDERS=0 8 * * *
CRON_DOCTOR_DIGEST=0 18 * * *

